[pytest]
testpaths = tests
markers =
    integration: tests requiring live network access (Playwright, real APIs); skip by default with `pytest -m "not integration"`
filterwarnings =
    ignore::DeprecationWarning

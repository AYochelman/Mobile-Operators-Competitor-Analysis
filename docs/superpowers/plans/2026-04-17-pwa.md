# PWA Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** הפוך את MOCA ל-installable PWA עם offline support — מציג מחירים אחרונים מה-cache כשאין חיבור.

**Architecture:** `vite-plugin-pwa` מייצר Service Worker דרך Workbox בזמן build. API calls מטופלות ב-NetworkFirst (cache fallback 24h). `useOnlineStatus` hook מזהה מצב רשת ומציג `OfflineBanner` כשהמשתמש offline.

**Tech Stack:** vite-plugin-pwa, Workbox, React hooks, Tailwind CSS

---

## File Map

| קובץ | פעולה | תוכן |
|------|--------|-------|
| `mass-market-app/vite.config.js` | Modify | הוספת VitePWA plugin עם Workbox runtimeCaching |
| `mass-market-app/src/hooks/useOnlineStatus.js` | Create | `navigator.onLine` + event listeners |
| `mass-market-app/src/components/OfflineBanner.jsx` | Create | Banner עם הודעת offline |
| `mass-market-app/src/App.jsx` | Modify | הוספת `<OfflineBanner />` |
| `mass-market-app/public/manifest.json` | Modify | הוספת description, categories, orientation |
| `mass-market-app/index.html` | Modify | תיקון apple-mobile-web-app-title |

---

## Task 1: Install vite-plugin-pwa and configure Workbox

**Files:**
- Modify: `mass-market-app/vite.config.js`
- Modify: `mass-market-app/package.json` (via npm install)

- [ ] **Step 1: Install the package**

```bash
cd "D:\השוואת MASS MARKET\mass-market-app"
npm install -D vite-plugin-pwa
```

Expected: `added 1 package` (or similar), no errors.

- [ ] **Step 2: Update vite.config.js**

Replace the full content of `mass-market-app/vite.config.js` with:

```js
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'
import { VitePWA } from 'vite-plugin-pwa'

export default defineConfig({
  plugins: [
    react(),
    tailwindcss(),
    VitePWA({
      registerType: 'autoUpdate',
      includeAssets: ['favicon.svg', 'icons/*.png', 'logos/*.png'],
      manifest: false,
      workbox: {
        globPatterns: ['**/*.{js,css,html,svg,png}'],
        runtimeCaching: [
          {
            urlPattern: /\/api\/(plans|abroad-plans|global-plans|content-plans|changes|abroad-changes)/,
            handler: 'NetworkFirst',
            options: {
              cacheName: 'api-data-cache',
              networkTimeoutSeconds: 5,
              expiration: { maxEntries: 20, maxAgeSeconds: 86400 },
              cacheableResponse: { statuses: [0, 200] }
            }
          },
          {
            urlPattern: /\/(banners|logos|icons)\//,
            handler: 'CacheFirst',
            options: {
              cacheName: 'static-assets-cache',
              expiration: { maxEntries: 100, maxAgeSeconds: 604800 },
              cacheableResponse: { statuses: [0, 200] }
            }
          }
        ]
      }
    })
  ],
  server: {
    proxy: {
      '/api': {
        target: 'http://localhost:5000',
        changeOrigin: true
      },
      '/banners': {
        target: 'http://localhost:5000',
        changeOrigin: true
      },
      '/archive-banners': {
        target: 'http://localhost:5000',
        changeOrigin: true
      }
    }
  }
})
```

- [ ] **Step 3: Build and verify sw.js is generated**

```bash
cd "D:\השוואת MASS MARKET\mass-market-app"
npm run build
```

Expected output includes:
```
✓ built in ...
```

Then verify the SW file exists:
```bash
dir dist\sw.js
```

Expected: file exists, size > 0.

- [ ] **Step 4: Commit**

```bash
cd "D:\השוואת MASS MARKET\mass-market-app"
git add package.json package-lock.json vite.config.js
cd "D:\השוואת MASS MARKET"
git commit -m "feat(pwa): add vite-plugin-pwa with Workbox NetworkFirst caching"
```

---

## Task 2: useOnlineStatus hook + OfflineBanner component

**Files:**
- Create: `mass-market-app/src/hooks/useOnlineStatus.js`
- Create: `mass-market-app/src/components/OfflineBanner.jsx`

- [ ] **Step 1: Create useOnlineStatus.js**

Create file `mass-market-app/src/hooks/useOnlineStatus.js` with exact content:

```js
import { useState, useEffect } from 'react'

export function useOnlineStatus() {
  const [isOnline, setIsOnline] = useState(navigator.onLine)

  useEffect(() => {
    const handleOnline  = () => setIsOnline(true)
    const handleOffline = () => setIsOnline(false)

    window.addEventListener('online',  handleOnline)
    window.addEventListener('offline', handleOffline)

    return () => {
      window.removeEventListener('online',  handleOnline)
      window.removeEventListener('offline', handleOffline)
    }
  }, [])

  return isOnline
}
```

- [ ] **Step 2: Create OfflineBanner.jsx**

Create file `mass-market-app/src/components/OfflineBanner.jsx` with exact content:

```jsx
import { useOnlineStatus } from '../hooks/useOnlineStatus'

export default function OfflineBanner() {
  const isOnline = useOnlineStatus()
  if (isOnline) return null
  return (
    <div className="w-full bg-amber-50 border-b border-amber-200 px-4 py-2 text-center text-sm text-amber-800 font-medium">
      ⚡ אין חיבור לאינטרנט — מציג נתונים אחרונים
    </div>
  )
}
```

- [ ] **Step 3: Lint check**

```bash
cd "D:\השוואת MASS MARKET\mass-market-app"
npm run lint
```

Expected: no errors related to the new files.

- [ ] **Step 4: Commit**

```bash
cd "D:\השוואת MASS MARKET\mass-market-app"
git add src/hooks/useOnlineStatus.js src/components/OfflineBanner.jsx
cd "D:\השוואת MASS MARKET"
git commit -m "feat(pwa): add useOnlineStatus hook and OfflineBanner component"
```

---

## Task 3: Wire OfflineBanner into App + update manifest + index.html + final build

**Files:**
- Modify: `mass-market-app/src/App.jsx`
- Modify: `mass-market-app/public/manifest.json`
- Modify: `mass-market-app/index.html`

- [ ] **Step 1: Add OfflineBanner to App.jsx**

In `mass-market-app/src/App.jsx`, add the import at the top (after existing imports):

```js
import OfflineBanner from './components/OfflineBanner'
```

Then in the `App` return, add `<OfflineBanner />` as the first child of `<ScrapeProvider>`, before `<Routes>`:

```jsx
export default function App() {
  return (
    <ScrapeProvider>
      <OfflineBanner />
      <Routes>
        <Route path="/login" element={<LoginPage />} />
        <Route path="/" element={<ProtectedRoute><Layout /></ProtectedRoute>}>
          <Route index element={<DashboardPage />} />
          <Route path="compare" element={<ComparePage />} />
          {/* trends page removed */}
          <Route path="alerts" element={<AlertsPage />} />
          <Route path="executive-summary" element={<ExecutiveSummaryPage />} />
          <Route path="archive" element={<ArchivePage />} />
          <Route path="settings" element={<ProtectedRoute adminOnly><SettingsPage /></ProtectedRoute>} />
        </Route>
        <Route path="*" element={<NotFoundPage />} />
      </Routes>
    </ScrapeProvider>
  )
}
```

- [ ] **Step 2: Update manifest.json**

Replace the full content of `mass-market-app/public/manifest.json` with:

```json
{
  "name": "MOCA — Mobile Operators Competitor Analysis",
  "short_name": "MOCA",
  "description": "השוואת מחירי חבילות סלולר ו-eSIM גלובלי לישראלים",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#f5ede0",
  "theme_color": "#5c3317",
  "lang": "he",
  "dir": "rtl",
  "orientation": "portrait-primary",
  "categories": ["utilities", "finance"],
  "icons": [
    { "src": "/icons/icon-192.png", "sizes": "192x192", "type": "image/png", "purpose": "any maskable" },
    { "src": "/icons/icon-512.png", "sizes": "512x512", "type": "image/png", "purpose": "any maskable" }
  ]
}
```

- [ ] **Step 3: Update index.html apple title**

In `mass-market-app/index.html`, find line:
```html
<meta name="apple-mobile-web-app-title" content="Mobile Operators Competitor Analysis" />
```

Replace with:
```html
<meta name="apple-mobile-web-app-title" content="MOCA" />
```

- [ ] **Step 4: Build and verify**

```bash
cd "D:\השוואת MASS MARKET\mass-market-app"
npm run build
```

Expected: build succeeds with no errors.

Verify SW and manifest in dist:
```bash
dir dist\sw.js dist\manifest.json
```

Expected: both files exist.

Open `dist/manifest.json` and confirm it contains `"description"` and `"orientation"` fields.

- [ ] **Step 5: Commit**

```bash
cd "D:\השוואת MASS MARKET\mass-market-app"
git add src/App.jsx public/manifest.json
cd "D:\השוואת MASS MARKET"
git add mass-market-app/index.html
git commit -m "feat(pwa): wire OfflineBanner, update manifest and apple title"
```

---

## Manual Verification Checklist

לאחר הפריסה ל-Netlify:

- [ ] Chrome DevTools → Application → Service Workers: מציג SW רשום
- [ ] Application → Manifest: מציג את כל השדות כולל description
- [ ] Network tab → Offline checkbox → רענן דף: מציג נתונים + banner amber
- [ ] חזרה ל-Online: banner נעלם
- [ ] Android Chrome: "Add to Home Screen" prompt מופיע לאחר 2-3 ביקורים
- [ ] iOS Safari: Share → "Add to Home Screen" → אייקון MOCA מופיע

---

## הערות

- `manifest: false` ב-VitePWA מונע ממנו לדרוס את `public/manifest.json` הקיים
- `networkTimeoutSeconds: 5` מונע המתנה של 30 שניות לרשת לפני fallback ל-cache
- `registerType: 'autoUpdate'` מעדכן SW ב-background — המשתמש לא צריך לעשות כלום
- הבנר מוצג גם בדף login — מכוון, כי המשתמש צריך לדעת שהוא offline גם לפני שהתחבר
